#include<iostream>
using namespace std;


// 1. Bitwise AND or & : For all 1's ans is 1 otherwise 0. Calculated on binary values.
// 2. Bitwise OR or | : For all 0's ans is 0 otherwise 1. Calculated on binary values.
// 3. Bitwise XOR or ^ : For all same bits ans is 0 otherwise 1. Calculated on binary values.
// 4. Bitwise Left Shift or << : If x << y then the leading y digits of binary representation of x will move to the start and all the bits move towards left by y bits. 
                                // Calculated on binary values.
                                // x << y (Left Shift operator) is equivalent to x*2^y.
// 5. Bitwise Right Shift or >> : If x >> y then the trailing y digits of binary representation of x will move to the end and all the bits move towards right by y bits.
                                // Calculated on binary values.
                                // x >> y (Left Shift operator) is equivalent to x/2^y.
// 6. Bitwise NOT or ~ : It is a unary operator. It inverse all the bits of a number.
                                // Calculated on binary values.
                                // This operator reduces the no from the largest possible value (INT_MAX) of that data-type and returns it.



int main(){

}